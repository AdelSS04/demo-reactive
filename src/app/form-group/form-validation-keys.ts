/**
 * Union type defining all possible validation error keys.
 * Used for type-safe error handling and consistent error messaging.
 */
export type FormValidationErrorKeys = 'required' | 'validNumber' | 'validDate' | 'validEmail' | 'validCountry'| 'validZipCode';
